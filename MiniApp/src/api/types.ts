export type CommunityMode = "Club" | "Camp";
export type Community = { key: string; name: string; mode: CommunityMode; timeZoneId: string; startsAtUtc?: string; endsAtUtc?: string; startDate?: string; endDate?: string; avatarUrl?: string };
export type Bootstrap = { canOpenAdminPanel: boolean; isSuperAdmin: boolean; communities: Community[] };
export type Capabilities = { boardGameGeekAvailable: boolean; boardGameGeekUnavailableReason?: string };
export type Profile = { botStartRequired?: boolean; startUrl?: string; preferredDisplayName?: string; telegramDisplayName: string; telegramUsername?: string };
export type ProfileGathering = { publicId: string; communityKey: string; communityName: string; communityMode: CommunityMode; gameName: string; startsAtUtc: string; localDate: string; localTime: string; localDateTime: string; isOrganizer: boolean };
export type ScheduleConflict = { publicId: string; gameName: string; startsAtUtc: string; communityKey: string; community: string; timeZoneId: string };
export type ApiErrorBody = { conflicts?: ScheduleConflict[]; code?: string; message?: string; currentRevision?: number; requiredDate?: string; affectedGatherings?: { publicId: string; gameName: string; startsAtUtc: string }[] };
export type Expansion = { bggId: number; name: string; originalName?: string };
export type GameType = "Strategy" | "Family" | "Party" | "Thematic" | "Abstract" | "War" | "Children" | "Customizable" | "Other";
export type TaxonomyItem = { bggId: number; name: string };
export type ClubGame = { bggId: number; name: string; originalName?: string; thumbnailImageUrl?: string; imageUrl?: string; description?: string; yearPublished?: number; minPlayers?: number; maxPlayers?: number; playerRangeDefaulted?: boolean; bestPlayers?: string; minPlayTimeMinutes?: number; maxPlayTimeMinutes?: number; minAge?: number; type?: GameType; typeName?: string; typeNames?: string[]; types?: string[]; categories?: string[]; categoryNames?: string[]; categoryItems?: TaxonomyItem[]; mechanics?: TaxonomyItem[]; mechanicNames?: string[]; expansions: Expansion[] };
export type ClubCollectionState = { collection: { version: number; games: ClubGame[] }; revision: number; updatedAt: string };
export type BggDetails = { game: ClubGame; expansions: Expansion[] };
export type BggBaseGameSearchResult = { bggId: number; name: string; originalName?: string; yearPublished?: number };
export type RecruitmentState = { priority: number; text: string; freeSeats: number; belowDesired: boolean };
export type GatheringPresentation = { recruitment?: RecruitmentState; publicId: string; gameName: string; imageUrl?: string; description?: string; organizerName: string; canTeachRules: boolean; rulesText: string; localDateTime: string; occupiedSeats: number; statusText: string; typeName?: string; bggId?: number; bggUrl?: string };
export type GatheringCard = GatheringPresentation & { maximumPlayers: number };
export type GatheringListItem = { card: GatheringCard; isOrganizer: boolean };
export type GatheringListPage = { items: GatheringListItem[]; page: number; hasPrevious: boolean; hasNext: boolean };
export type GatheringDetail = { canRequestRecruitment?: boolean; recruitmentDelivery?: string; provider?: GameProvider; canRecordPlay?: boolean; botStartRequired?: boolean; startUrl?: string; gathering: GatheringPresentation & { expansions: string[]; typeNames?: string[]; categoryNames?: string[]; mechanicNames?: string[] }; status: string; currentUserStatus: string; canEdit: boolean; canClose: boolean; canReopen: boolean; canCancel: boolean; canManageGuests: boolean; hasStarted: boolean; canJoin: boolean; canLeave: boolean; waitlistPosition?: number; confirmedParticipants: { name: string; isOrganizer: boolean; contactUrl?: string }[]; guestParticipants: { id: number; displayName: string }[]; waitlistedParticipants: { name: string; position: number; contactUrl?: string }[]; publicationStatus: string; publicationError?: string; canRetryPublication: boolean; startsAtLocal: string; minimumPlayers: number; desiredPlayers: number; maximumPlayers: number; gameMinimumPlayers?: number; gameMaximumPlayers?: number; gamePlayerRangeDefaulted?: boolean; description?: string; canTeachRules: boolean; knownExpansions: Expansion[]; selectedExpansionIds: number[] };
export type GameSnapshot = { name: string; originalName?: string; thumbnailImageUrl?: string; imageUrl?: string; minPlayers?: number; maxPlayers?: number; bestPlayers?: string; type?: GameType; types?: string[]; categories?: string[] };
export type Contribution = { bggId: number; itemType: "BaseGame" | "Expansion"; parentBggId?: number; source: "Legacy" | "BggImport" | "Manual"; commitment: "Available" | "Bringing"; snapshot: GameSnapshot };
export type CatalogProvider = { participantId?: number; displayName: string; city?: string; contactUrl?: string; commitment: "Available" | "Bringing"; isCurrentUser: boolean };
export type GameListItem = { isWished?: boolean; canWish?: boolean; scheduledGatherings?: number; recordedPlays?: number; bggId: number; name: string; originalName?: string; thumbnailImageUrl?: string; type: GameType; typeName: string; typeNames: string[]; minPlayers?: number; maxPlayers?: number; bestPlayers?: string; availabilitySummary: string; isDefinitelyAvailable: boolean; needsProviderCoordination: boolean };
export type CatalogResponse = { items: GameListItem[]; filters: { categories: TaxonomyItem[]; types: { key: GameType; value: string }[] } };
export type GameDetails = { isWished?: boolean; canWish?: boolean; bggId: number; name: string; originalName?: string; imageUrl?: string; description?: string; yearPublished?: number; type: GameType; typeName: string; typeNames: string[]; minPlayers?: number; maxPlayers?: number; bestPlayers?: string; minPlayTimeMinutes?: number; maxPlayTimeMinutes?: number; minAge?: number; categories: TaxonomyItem[]; mechanics: TaxonomyItem[]; expansions: Expansion[]; bggUrl: string; availability: { isOwned?: boolean; isInBaseCollection: boolean; providers: CatalogProvider[]; hasCommittedProvider: boolean } };
export type ImportDraftItem = { bggId: number; itemType: "BaseGame" | "Expansion"; parentBggId?: number; parentBggIds?: number[]; snapshot: GameSnapshot; selectedByDefault: boolean; skipReason?: "AlreadyInBaseCollection" | "AlreadyAddedManually" | "InvalidOrUnsupportedItem" | "ProviderDataIncomplete"; isOverridable: boolean };
export type CampImport = { publicId: string; status: string; bggUsername: string; progressCurrent: number; progressTotal?: number; draft?: { items: ImportDraftItem[] }; error?: string; updatedAt: string; expiresAt: string; overrideResolution?: "KeepBaseCollection" | "AddPersonalCopies"; hasSelectedOverridableItems: boolean };
export type PeerTicket = { publicId: string; purpose: string; status: string; preparedButtonId?: string; expiresAt: string; result?: { users?: { telegramUserId: number; displayName?: string; username?: string }[]; chat?: { telegramChatId: number; title?: string; username?: string } } };
export type AdminOverview = { clubs: AdminClub[]; camps: AdminCamp[]; lockedCommunities: LockedAdminCommunity[]; isSuperAdmin: boolean };
export type LockedAdminCommunity = { communityKey?: string; telegramChatId: number; name: string; mode: CommunityMode; isActive: boolean; isApproved: false };
export type AdminClub = { id: number; communityKey: string; name: string; telegramTitle: string; telegramChatId: number; timeZoneId: string; isActive: boolean; isApproved: true; gameCount: number; collectionRevision: number; updatedAt: string; gatherings: number };
export type AdminCamp = { id: number; communityKey: string; name: string; telegramTitle: string; telegramChatId: number; timeZoneId: string; isApproved: true; status: string; startsAtUtc?: string; endsAtUtc?: string; startDate?: string; endDate?: string; sourceClubId?: number; sourceClubName?: string; registrations: number; contributions: number; gatherings: number };
export type CampAdminParticipant = { participantId: number; displayName: string; city?: string; selectedDates: string[]; needsAccommodation: boolean; telegramUsername?: string; contactUrl?: string };
export type CampAdminParticipants = { campId: number; campName: string; participants: CampAdminParticipant[] };
export type CampParticipantDmResult = { messageCount: number; participantCount: number };
export type Administrator = { telegramUserId: number; displayName?: string; telegramUsername?: string; grantedByTelegramUserId: number; createdAt: string };
export type EligibleAdministrator = { telegramUserId: number; displayName?: string; telegramUsername?: string };
export type PostingTopicSettings = { isForum: boolean; messageThreadId: number | null; topicName: string | null; needsSelection: boolean; knownTopics: { messageThreadId: number; name: string; isClosed: boolean }[] };

export type PersonalCollectionItem = Omit<Contribution, "commitment">;

export type GameProvider = { state: string; summary: string; providers: CatalogProvider[]; canBring: boolean; isConfirmed: boolean; isOwned?: boolean };
