export type CommunityMode = "Club" | "Camp";
export type Community = { key: string; name: string; mode: CommunityMode; timeZoneId: string };
export type Bootstrap = { isAdministrator: boolean; communities: Community[] };
export type Capabilities = { boardGameGeekAvailable: boolean; boardGameGeekUnavailableReason?: string; preparedPeerSelection: boolean };
export type ApiErrorBody = { code?: string; message?: string; currentRevision?: number };
export type Expansion = { bggId: number; name: string };
export type ClubGame = { bggId: number; name: string; thumbnailImageUrl?: string; imageUrl?: string; minPlayers?: number; maxPlayers?: number; bestPlayers?: string; expansions: Expansion[] };
export type ClubCollectionState = { collection: { version: number; games: ClubGame[] }; revision: number; updatedAt: string };
export type BggDetails = { game: ClubGame; expansions: Expansion[] };
export type GatheringCard = { publicId: string; gameName: string; imageUrl?: string; description?: string; rulesText: string; localDateTime: string; confirmedPlayers: number; desiredPlayers: number; maximumPlayers: number; statusText: string };
export type GatheringListItem = { card: GatheringCard; status: string; publicationStatus: string; isOrganizer: boolean };
export type GatheringDetail = { gathering: GatheringCard & { organizerName: string; minimumPlayers: number; expansions: string[] }; status: string; currentUserStatus: string; canManage: boolean; canJoin: boolean; canLeave: boolean; waitlistPosition?: number; confirmedParticipants: { name: string; isOrganizer: boolean }[]; waitlistedParticipants: { name: string; position: number }[]; publicationStatus: string; publicationError?: string; canRetryPublication: boolean; startsAtLocal: string; minimumPlayers: number; desiredPlayers: number; maximumPlayers: number; description?: string; canTeachRules: boolean; knownExpansions: Expansion[]; selectedExpansionIds: number[] };
export type Contribution = { bggId: number; itemType: "BaseGame" | "Expansion"; parentBggId?: number; source: "Legacy" | "BggImport" | "Manual"; snapshot: { name: string; thumbnailImageUrl?: string } };
export type ImportDraftItem = { bggId: number; itemType: "BaseGame" | "Expansion"; parentBggId?: number; snapshot: { name: string; thumbnailImageUrl?: string }; selectedByDefault: boolean };
export type CampImport = { publicId: string; status: string; bggUsername: string; progressCurrent: number; progressTotal?: number; draft?: { items: ImportDraftItem[] }; error?: string; updatedAt: string; expiresAt: string };
export type PeerTicket = { publicId: string; purpose: string; status: string; preparedButtonId?: string; expiresAt: string; result?: unknown };
export type AdminOverview = { clubs: AdminClub[]; camps: AdminCamp[] };
export type AdminClub = { id: number; name: string; telegramTitle: string; timeZoneId: string; isActive: boolean; gameCount: number; collectionRevision: number; updatedAt: string };
export type AdminCamp = { id: number; name: string; telegramTitle: string; timeZoneId: string; status: string; startDate?: string; endDate?: string; sourceClubName?: string; registrations: number; contributions: number; gatherings: number };
export type Administrator = { telegramUserId: number; displayName?: string; telegramUsername?: string; createdAt: string };
